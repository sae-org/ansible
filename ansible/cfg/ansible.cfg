[defaults]
inventory = ./inventory/aws_ec2.yml    # points to your dynamic AWS inventory file
host_key_checking = False               # skip the SSH fingerprint "yes/no" prompt
retry_files_enabled = False             # don’t clutter with .retry files on failure
interpreter_python = auto_silent        # auto-detect remote Python path silently
timeout = 30                            # SSH connection timeout (in seconds)

[inventory]
enable_plugins = amazon.aws.ec2         # enable the AWS EC2 dynamic inventory plugin